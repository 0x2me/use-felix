import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Token Tracker | Monitor Your Crypto Portfolio",
  description: "Track your cryptocurrency token balances and portfolio value in real-time. Monitor ETH, WBTC, USDC, and more with live blockchain data and price updates.",
  keywords: ["crypto", "token tracker", "portfolio", "ethereum", "web3", "blockchain", "defi"],
  authors: [{ name: "Token Tracker" }],
  openGraph: {
    title: "Token Tracker",
    description: "Track your cryptocurrency portfolio in real-time",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Providers>
          {children}
        </Providers>
        <Toaster />
      </body>
    </html>
  );
}
